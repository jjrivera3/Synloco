export interface User {
  id: string;
  name: string;
  imageUrl?: string;
  clerkUserId?: string; // Add other fields if needed
  email?: string;
  createdAt?: string;
  updatedAt?: string;
  userId?: string;
}
